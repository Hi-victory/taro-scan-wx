import { Text, View } from "@tarojs/components";

const NoMore = () => {
  return (
    <View
      style={{
        width: "100%",
        textAlign: "center",
        padding: "12px 0",
        background: "#f0f0f0",
      }}
    >
      <Text style={{ fontSize: "12px", color: "#8c8c8c" }}>没有更多了</Text>
    </View>
  );
};

export default NoMore;
